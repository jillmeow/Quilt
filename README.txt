Quilting Pattern
By Jill Mirandilla

This program will produce several rectangles that form a quilting pattern

Compile the program first by typing the command:
javac *.java

Run the program by typing the command:
java Quilt < [filename in txt]

example: java Quilt < basic.txt
